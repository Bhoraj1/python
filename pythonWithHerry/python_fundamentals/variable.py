age = 18 #integer
name = "Bhoraj" #String
pi = 2.14 #float

#Rules of defining a variable in Python
# Variable names must start with a letter (a-z,A-Z) or underscore(_).
# They can contain letters,number,and underscrores.
# Variable names are case-sensitive(age,and Age are different)
# Avoid using python keybaords(e.g if,for , while) as variable names.

age = 18
print(type(age))